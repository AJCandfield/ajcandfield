---
name: Metrics
concurrency: main
on:
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:

jobs:
  metrics:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Github metrics
        uses: lowlighter/metrics@latest
        with:
          token: ${{ secrets.PROFILE_METRICS }}
